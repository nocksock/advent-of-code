fn main() {

    //     .map(|(left, right)| {
    //         left.map(|compartment| compartment.split("").collect::<String>())
    //             .collect()
    //         // let a = HashSet::from(left.split("").into());
    //         // let b = HashSet::from(right.split("").into());
    //         // return a.intersection(&b).into_iter().collect::<String>();
    //     })
    //     .collect();
    //

}
